Every time you do a discovery on something very important, save it in the MEMORY.md document.
Read from MEMORY.md every time you start a new action.

This project is targeted to PHP 8.5+
Any tool or software you don't find (or has a wrong version) from the shell, invoke it via docker.